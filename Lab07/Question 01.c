#include <stdio.h>
int main()
{
    /*
    //declare the array
    int arr[10],i;
    //input
    for(i=0;i<10;i++)
    {
        printf("Enter a value to the element no. %d - ",i+1);
        scanf("%d",&arr[i]);
    }
    //output
    for(i=0;i<10;i++)
    {
        printf("Element %d contains %d\n",i+1,arr[i]);
    }


    //Task 02
    int x;
    float avg,arr[10],sum=0,max;

    for(x=0;x<10;x++)
    {
        printf("Enter no.%d product's price - ",x+1);
        scanf("%f",&arr[x]);
    }

    for(x=0;x<10;x++)
    {
        sum+=arr[x];
    }
    avg=sum/10;
    printf("The average of all the items is %.2f\n",avg);

    max=arr[0];
    for(x=1;x<10;x++)
    {
        if(arr[x]>max)
        {
            max=arr[x];
        }
    }
    printf("The highest price is %.2f\n",max);

    for(x=0;x<10;x++)
    {
        printf("Item no.%d - %.2f\n",x+1,arr[x]);

    }
    */


}
